package com.example.whostolemyfood.menu.domain.entity;

public class DescriptionEntity {
    // description_id
    // menu_id
    // ai_log_id
    // description String
    // (cre, upd, del)_at / (cre, upd, del)_by
    // is_ai enum
}
